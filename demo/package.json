{
  "name": "fhirpath-ts-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Client-only playground for fhirpath-ts, deployable to Cloudflare Pages.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist --project-name fhirpath-ts-demo"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.0.3"
  }
}
