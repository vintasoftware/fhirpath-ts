[package]
name = "rs-harness"
version = "0.1.0"
edition = "2021"

[dependencies]
octofhir-fhirpath = "=0.4.50"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros"] }

[profile.release]
opt-level = 3
lto = true
