// Compiles only the type-level perf fixture, so scripts/check-type-perf.ts
// can measure inference cost in isolation from the rest of the project.
{
  "extends": "./tsconfig.json",
  "include": ["src/typed/verification/perf-fixture.types.ts"],
  "exclude": []
}
